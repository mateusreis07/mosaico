name: mosaico
description: "A Human Pixel Mosaico App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core
  flutter_riverpod: ^2.4.9
  go_router: ^13.1.0
  google_fonts: ^6.1.0
  
  # Features
  mobile_scanner: ^5.0.0
  http: ^1.2.0
  wakelock_plus: ^1.1.0
  screen_brightness: ^0.2.1
  permission_handler: ^11.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
